import * as mathFunctions from '../utils/math';

//Call the square function and log the result
console.log('square of 5:', mathFunctions.calculateSquare(5));

//Call the triangleArea function and log the result
console.log('area of triangle with base 4 and height 5:', mathFunctions.calculateTriangleArea(4, 5));